# User Guide
Version: v1.0

## 1. Принципы: Inbox / принятие решений

### 1.1 Что это за система
«Мой ежедневник-органайзер» — это личная система управления, а не список дел.

Она нужна не для того, чтобы:

мотивировать;

контролировать;

оптимизировать твоё время автоматически;

подсказывать, как правильно жить.

Она нужна, чтобы:

не терять входящий поток;

разделять мышление и исполнение;

принимать управленческие решения осознанно;

видеть реальную картину обязательств.

### 1.2 Главное правило системы
Если факт не зафиксирован в системе — его не существует.

Telegram, голос, календарь — это интерфейсы.
Источник истины — сама система.

## 1.6 LLM Router (CLI)
Разница режимов:
- `voice_command` / `text_command` — короткая исполнимая команда (create/update/list).
- `assistant` — обзор/план/риски/предложения без исполнения.
assistant не имеет schema; выдаёт JSON-only по контракту.
suggested_command (если есть) — опционален, совместим с `schemas/command.schema.json`, не исполняется автоматически.
intent=unknown означает уточнение (needs_clarification=true, clarifying_question обязателен).

PowerShell env:
```powershell
$env:OPENROUTER_API_KEY="your_key"
$env:OPENROUTER_BASE_URL="https://openrouter.ai/api/v1"
$env:OPENROUTER_HTTP_TIMEOUT_S="60"
$env:OPENROUTER_MODEL_COMMANDS="nvidia/llama-3.1-nemotron-nano-8b-v1:free"
$env:OPENROUTER_MODEL_ASSISTANT="nvidia/llama-3.1-nemotron-nano-8b-v1:free"
```

CLI:
```bash
python -m src.llm.router --kind voice_command --text "создай задачу купить молоко завтра" --now "2026-02-08T12:00:00+03:00"
python -m src.llm.router --kind assistant --text '{...snapshot...}' --now "2026-02-08T12:00:00+03:00"
```

Минимальный snapshot для assistant:
```json
{
  "now_iso": "2026-02-08T12:00:00+03:00",
  "stats": { "tasks_total": 10, "tasks_today": 3, "tasks_overdue": 1 },
  "projects": [{ "name": "Альфа", "tasks_open": 5, "overdue": 1 }],
  "top_tasks": [{ "title": "Позвонить Ивану", "due": "2026-02-08T18:00:00+03:00", "project": null }],
  "user_request": "Что лучше сделать сегодня?"
}
```

### 1.3 Inbox — обязательная точка входа
В систему попадает всё:

задачи;

мысли;

идеи;

направления;

голосовые заметки.

Пока у записи:

нет даты;

нет решения, что с ней делать —

она находится в Inbox.

Inbox — это не список задач.
Это буфер для принятия решений.

### 1.4 Как работать с Inbox
Inbox — это не список задач.
Это место, где ты принимаешь решения.

Каждая запись в Inbox требует одного из действий:

превратить в проект,

добавить задачу,

запланировать,

оставить без даты,

ничего не делать.

### 1.5 Как планировать задачи из Inbox
Если задача без даты, доступны действия:

Запланировать

Через 1 день

Через 3 дня

Выбрать дату

Оставить без даты

Напомнить через 7 дней

быстрый вариант “убрать из головы”

ставит напоминание и убирает задачу из Inbox

Если задача получила дату — она исчезает из Inbox и переходит в операционный контур.

## 2. Цели периода

P3.2 — Цели периода
Канон постановки и использования

Версия: v1.0
Статус: канон проекта
Назначение: зафиксировать, что такое цель периода, как она появляется и как с ней работать

1. Что такое цель периода

Цель периода — это зафиксированное направление управленческого воздействия,
которое вытекает из анализа предыдущего периода и имеет измеримый критерий (KPI).

Цель:

не является задачей;

не является проектом;

не является планом;

не является обещанием результата.

Цель — это на что мы сознательно влияем в этом периоде.

2. С чего начинается цель

Цель не придумывается.
Она всегда начинается с анализа.

Анализ — это не таблицы и не графики

Анализ — это ответ на один вопрос:

Почему мы получили именно такой результат, а не другой?

Анализ обязателен:

при плохом результате;

при хорошем результате (особенно).

Важно понять:

это следствие наших действий;

или внешних условий;

или случайности.

Анализ — это действие пользователя, а не сущность системы.

3. Управленческий вывод

Из анализа появляется управленческий вывод:

На что мы реально можем влиять, чтобы изменить результат?

Именно этот вывод и фиксируется как цель периода.

4. KPI в цели периода (обязательно)

Цель периода всегда имеет KPI, потому что без него невозможно понять,
достигнута цель или нет.

При этом:

KPI — это критерий оценки;

KPI — не план достижения;

KPI — не список действий.

Примеры

❌ Неправильно

Увеличить средний чек

✅ Правильно

Увеличить долю допродаж
(KPI: средний чек)

❌ Неправильно

Навести порядок в администрировании

✅ Правильно

Стабилизировать выполнение регламентов
(KPI: % заполненных журналов)

5. Может ли цель существовать без плана

Да. И это принципиально.

Правильная формулировка канона:

Цель периода не может существовать без KPI,
но может существовать без формализованного плана.

Почему:

не каждое управленческое воздействие требует проекта;

иногда достаточно:

управленческого внимания,

контроля,

корректировок по ходу периода.

Проект или задачи создаются только тогда,
когда управленческого внимания недостаточно.

6. Чем цель отличается от проекта
Цель периода

появляется из анализа;

отвечает на вопрос «на что мы влияем»;

имеет KPI;

может длиться несколько периодов;

может не порождать действий.

Проект

появляется из решения «делаем вот это»;

отвечает на вопрос «что именно делаем»;

имеет начало и конец;

почти всегда дробится на задачи.

Коротко:

Цель — куда давим.
Проект — как именно давим.

7. Жизненный цикл цели
В конце периода цель всегда получает один из статусов:

Продлить — цель остаётся актуальной;

Достигнута — цель закрывается;

Снять — цель больше не имеет смысла.

Отсутствие решения — тоже решение:
цель остаётся активной.

8. Связь цели с задачами и проектами

цель может породить задачи;

цель может породить проект;

цель может не породить ничего — это нормально.

Цель удерживает внимание.
Действия появляются только при необходимости.

9. Типичные ошибки (что не делать)

❌ превращать цель в список задач
❌ формализовать план “на всякий случай”
❌ требовать выполнения цели как задачи
❌ оценивать цель ежедневно или еженедельно
❌ подменять анализ отчётами

10. Ключевой принцип

Цели не выполняются.
Они удерживаются во времени.

## 3. Управленческие циклы (месяц/квартал)

### 3.1 Управленческие циклы
Есть отдельный класс действий — управленческие циклы.

Это не задачи и не проекты.

Это то, что ты и так делаешь регулярно:

подведение итогов;

анализ показателей;

постановка целей;

корректировка фокуса.

Управленческий цикл:

не имеет дедлайна «сделать»;

не является задачей;

нужен, чтобы принять решения.

Результат управленческого цикла:

цели;

направления;

проекты;

операционные задачи.

### 3.2 Как всё связано между собой
В системе нет одной цепочки.
Есть несколько источников операционных задач.

Из направлений:
Направление
    ↓ (если принято решение)
  Проект
    ↓
  Задачи

Из управленческих циклов:
Управленческий цикл
    ↓
  Решения / цели
    ↓
  Задачи / проекты

Операционный контур:
Задача → Планирование → Действие

### 3.3 Операционный контур (исполнение)
Операционный контур — это всё, что связано с исполнением:

планирование задач;

переносы;

снятие с плана;

выполнение;

работа с перегруженными днями.

Здесь действует ключевое правило:

Задача без времени — источник скрытого стресса.

### 3.4 Важное различие: «Снять с плана» и «Отменить»
Это два разных управленческих действия.

Снять с плана

я не делаю это сейчас;

вернусь позже;

обязательство по времени снимается;

задача остаётся.

Отменить задачу

я больше не собираюсь это делать;

обязательство снимается навсегда;

задача остаётся в истории как факт отмены.

### 3.5 Чего система принципиально не делает
Система:

не переносит задачи сама;

не оптимизирует расписание;

не подменяет твои решения;

не даёт советов «как лучше»;

не превращается в коуча.

Она подсвечивает последствия, но не решает за тебя.

### 3.6 Как пользоваться системой правильно
Коротко:

Всё фиксируй — не держи в голове.

Через Inbox решай, что это:

задача;

направление;

ничего.

Не тащи большие смыслы в календарь.

Планируй только то, что собираешься делать.

Регулярно проходи управленческие циклы.

Помни: не всё большое — проект, и не всё важное — задача.

### 3.7 Главное
Эта система не делает тебя более дисциплинированным.
Она делает твоё управление честным, прозрачным и осознанным.

## 4. Регламенты (monthly obligations)

Регламенты — регулярные обязательства по месяцам.

Команда:

`/regs` (или `/reg`) — экран «Регламенты месяца».

На экране:

- список регламентов за выбранный месяц;
- статусы: 🔴 Не выполнен, 🟢 Выполнен, ⚠️ Пропущен;
- действия:
  - ✅ Выполнить
  - ⏭ Пропустить
  - ⛔ Отключить

Навигация:

- «Пред. месяц» / «След. месяц»
- «Обновить»

Подробности: `Docs/04_REGULATIONS.md`.

## 4.1 Сегодня (операционный обзор)

Команда:

`/today` (алиас `/status`) — единый экран «Сегодня».

Показывает:

- 📌 Задачи на сегодня (PLANNED/SCHEDULED)
- 📅 Регламенты на сегодня (DUE/OPEN)
- 📥 Backlog (NEW/IN_PROGRESS)
- ⚠️ Сигналы (режимы Drift/Overload/Nudges)

Кнопки:

- ✅ Готово (задачи на сегодня)
- ✅ Выполнить / ⏭ Пропустить (регламенты на сегодня)
- 🔄 Обновить / 📅 /regs / 📃 /list open

## 4.2 Экспорт

Доступны два read-only экспорта через organizer-api:

- `GET /export/json`
  - JSON с задачами (включая подзадачи), регламентами и запусками регламентов.
  - Метаданные: `exported_at` (UTC), `timezone` = `UTC`.
- `GET /export/csv`
  - ZIP-архив с файлами: `tasks.csv`, `subtasks.csv`, `regulations.csv`, `regulation_runs.csv`.
  - Кодировка UTF-8, разделитель запятая.

Фильтр по дате (опционально для обоих):

- `?from=YYYY-MM-DD&to=YYYY-MM-DD`
- применяется к `created_at` (UTC)
- включительно по границам

Примеры:

```text
GET /export/json?from=2026-02-01&to=2026-02-07
GET /export/csv?from=2026-02-01&to=2026-02-07
```

## 4.3 Управление блоками

Раздел доступен, когда включен `P7_MODE=on`.

В `/today` появляется секция:

⏱️ Блоки дня

Для каждого блока доступны действия:
- ⬅️ -10 / ➡️ +10 — сдвиг блока на 10 минут с соблюдением инвариантов;
- 🗑 Удалить — удалить блок.

Если сдвиг невозможен (пересечение или выход за границы дня), блок не меняется.

## 5. Сигналы состояния (overload/drift)

### 5.1 Что делать при перегрузке

Версия: v1.0
Назначение: зафиксировать корректное управленческое поведение при перегрузке
Статус: канон проекта

1. Что в системе считается перегрузкой

Перегрузка — это не количество задач и не чувство усталости.

Перегрузка фиксируется, когда:

входящие записи не превращаются в решения;

задачи не получают времени;

проекты висят без движения;

Inbox не разгружается.

Перегрузка — это управленческий сбой,
а не личная неэффективность.

2. Чего НЕ нужно делать при перегрузке

При перегрузке запрещено:

❌ хаотично переносить задачи;

❌ добавлять новые даты “на потом”;

❌ пытаться “оптимизировать календарь”;

❌ удалять задачи, чтобы стало легче;

❌ игнорировать Inbox неделями.

Эти действия маскируют проблему, но не решают её.

3. Единственно корректное действие

Если система показывает сигнал перегрузки:

Запустить управленческий цикл

Никаких других обязательных действий нет.

4. Как правильно реагировать на сигнал
Шаг 1. Остановиться

Не нужно ничего исправлять сразу.

Сигнал перегрузки — это приглашение к обзору, а не тревога.

Шаг 2. Выбрать

У сигнала всегда два варианта:

Начать обзор

Позже

Оба варианта корректны.

Если выбран вариант «Позже»

сигнал гасится;

система не давит;

ответственность остаётся у пользователя.

Это осознанный выбор.

5. Если запущен управленческий цикл
5.1 Смотреть только на факты

Во время цикла показываются:

количество unplanned-задач;

количество переносов;

количество активных проектов;

задачи без движения.

Важно:
Никаких оценок. Только факты.

5.2 Ответить на главный вопрос

Хочу ли я что-то изменить в текущих обязательствах?

Возможны оба ответа.

Если ответ «Нет»

цикл корректно завершается;

ничего не ломается;

это зафиксированное решение “оставить как есть”.

Если ответ «Да»

Допустимые действия:

зафиксировать новое направление;

поставить цель на период;

изменить или добавить задачи;

ничего не делать (тоже допустимо).

6. Что считается корректным результатом цикла

Корректный результат — это не обязательно изменения.

Цикл считается успешным, если:

пользователь осознанно посмотрел на факты;

принял решение (даже “ничего не менять”);

вернулся к операционке без давления.

7. Что делать после цикла

После завершения цикла:

вернуться к задачам;

продолжить работу;

не пересматривать всё подряд.

Если перегрузка повторяется — это повод:

не для срочных действий,

а для следующего управленческого цикла.

8. Чего система не будет делать вместо пользователя

Система никогда:

не отменит задачи сама;

не изменит приоритеты;

не “разгрузит” календарь;

не примет решения.

Система показывает последствия.
Решения остаются за пользователем.

9. Ключевой принцип регламента

Перегрузка — это сигнал к управлению,
а не к ускорению.

10. Статус регламента

Этот регламент:

обязателен для системы;

не навязывается пользователю;

используется как эталон поведения и UX.

### 5.2 Сигналы системы: перегрузка и drift

Статус: канон проекта
Назначение: объяснить, что означают сигналы системы и как с ними работать

1. Зачем этот документ

Эта система:

не управляет временем за тебя;

не оптимизирует твою загрузку;

не подсказывает «как правильно».

Она показывает состояние управления.

Этот документ нужен, чтобы:

понимать, что именно система тебе сигнализирует;

не путать перегрузку с drift;

не пытаться лечить симптомы вместо причин.

2. Три уровня того, что происходит

В системе всегда есть три параллельных слоя:

2.1 Исполнение

задачи;

сроки;

переносы;

выполнение.

Это операционный слой.

2.2 Управление

проекты;

направления;

управленческие циклы;

решения.

Это управленческий слой.

2.3 Состояние системы

перегрузка;

drift;

связность;

управляемость.

Это мета-уровень, который система и показывает сигналами.

3. Что такое перегрузка и как её читать
Перегрузка — это:

не «много задач»;

не усталость;

не плохая дисциплина.

Перегрузка — это сбой превращения входящего потока в решения.

Типовые признаки перегрузки

Inbox не разгружается;

много задач без дат;

частые переносы;

много начатого и мало завершённого.

Что означает сигнал перегрузки

«Текущие обязательства перестали быть управляемыми».

Это сигнал не к ускорению, а к управлению.

Единственно корректная реакция

Запустить управленческий цикл.

Можно:

начать обзор;

отложить.

Оба варианта допустимы.

4. Что такое drift и как его читать
Drift — это:

не перегрузка;

не хаос;

не ошибка планирования.

Drift — это расхождение между тем, что ты делаешь, и тем, что считаешь важным.

Типовые признаки drift

задачи не связаны с проектами;

проекты не связаны с направлениями;

направления не приводят к действиям;

циклы не дают последствий.

Что означает сигнал drift

«Исполнение живёт отдельно от смыслов».

Это сигнал не к чистке задач, а к пересборке связности.

Единственно корректная реакция

Запустить управленческий цикл (фокус: связность).

Можно:

начать обзор;

отложить.

Оба варианта допустимы.

5. Почему система не “лечит” сама

Система принципиально не делает следующее:

не отменяет задачи;

не перепривязывает их автоматически;

не закрывает проекты;

не решает, что важно.

Потому что:

управление ≠ автоматизация.

Система показывает последствия,
а не подменяет их.

6. Управленческий цикл — универсальный выход

И при перегрузке, и при drift:

нет разных кнопок;

нет разных режимов;

нет “специальных экранов”.

Есть один инструмент — управленческий цикл.

Что делает цикл

показывает факты;

задаёт один ключевой вопрос;

позволяет принять решение;

допускает ответ «ничего не менять».

Что цикл НЕ обязан делать

приводить к изменениям;

заканчиваться задачами;

улучшать метрики.

Цикл — это про осознанность, не про результат.

7. Как читать сигналы системы правильно

Коротко:

🔔 Перегрузка
→ проблема с количеством и управляемостью обязательств.

🔔 Drift
→ проблема со связностью и направлением усилий.

В обоих случаях:

система предлагает не действие, а обзор.

8. Что делать, если сигналы повторяются

Повторяющиеся сигналы означают:

не «ты что-то делаешь неправильно»;

а «система усложнилась».

Корректная реакция:

не чинить быстрее;

не добавлять правил;

не “дисциплинировать себя”.

А:

регулярно возвращаться к управленческим циклам.

9. Главный принцип документа

Система не оценивает тебя.
Она показывает, где именно теряется управление.

10. Статус документа

Этот документ:

объясняет логику сигналов;

используется как эталон UX и регламентов;

не предназначен для мотивации или обучения.
